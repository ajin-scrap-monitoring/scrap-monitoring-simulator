{
  "name": "scrap-monitoring-visualizer-web",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node node_modules/typescript/bin/tsc --project tsconfig.json && node scripts/bundle.mjs",
    "test": "rm -rf .test-dist && node node_modules/typescript/bin/tsc --project tsconfig.test.json && node --test .test-dist/tests/*.test.js"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "5.9.2"
  }
}
